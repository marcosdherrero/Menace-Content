# Unzipped world saves (use the world .zip on Releases instead)
Season 2/worlds/*/

# World / full dumps over git's 100 MB limit (world zip → Releases; ~29 GB tar → external host)
Season 2/worlds/*.zip
Season 2/worlds/*.tar
Season 2/worlds/*.tar.gz
Season 2/zipped_versions/**

# Individual jars over git's 100 MB limit (still in Extended release packs)
Season 2/mods/**/Flashback*.jar

# Client jars moved out of Menace_Mods_2_1 (folder is essential/server only)
Season 2/mods/_aside_client_*/

# Extended pack is ~450 MB (Flashback) — GitHub Releases only
Season 2/downloads/Menace.S2.1.7.Extended.Mods.zip

# Pack-build scratch (not shipped)
_staging/
_packmeta/
_lookup.json

# OS / editor junk
Thumbs.db
Desktop.ini
.DS_Store
*.tmp
github-share/
